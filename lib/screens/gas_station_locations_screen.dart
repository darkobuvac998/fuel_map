import 'package:flutter/material.dart';

class GasStationLocationsScreen extends StatelessWidget {
  const GasStationLocationsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Map'),
    );
  }
}
